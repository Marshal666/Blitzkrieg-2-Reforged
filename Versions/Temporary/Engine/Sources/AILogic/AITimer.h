#pragma once

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class CAITimer
{
	static interface IGameTimer *pTimer;
public:
	CAITimer();
	static NTimer::STime GetSegmentTime();
	static NTimer::STime GetGameTime(); // п ю я у н д х р я ъ ! ! ! ! ! рнкэйн дкъ Updater'Ю
	static void ToClientTime( NTimer::STime *pTime );
	static void SetSpeed( int nSpeed );
};
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////